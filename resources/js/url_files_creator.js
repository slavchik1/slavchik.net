const selectSystem = document.getElementById("select-system");
const url = document.getElementById("url");
const generate = getElementById("generate");
const downloadA = getElementById("download-a");
const downloadButton = getElementById("download-button");
